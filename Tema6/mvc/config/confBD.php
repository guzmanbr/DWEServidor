<?
    // Si queremos que nadie acceda, debería ir fuera de la carpeta servidor;

    // Define una constante con su valor || //.gitignore -> confBD.php
    define('IP', '192.168.7.208');  // Se podria usar el codigo de verCodigo, pero solo si el servidor y la BBDD se encuentran en el mismo sitio.
    define('BBDD', 'mvc');
    define('USER', 'maria');
    define('PASS', 'maria');

?>